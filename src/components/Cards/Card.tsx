import styled from "styled-components";

const Card = styled.div`
border: none;
  border-radius: 6px;
  padding: 1.5rem;
  box-shadow: var(--cvna-elevation-1);
  background: white;`;

export default Card;